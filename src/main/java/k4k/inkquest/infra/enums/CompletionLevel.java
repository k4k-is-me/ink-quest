package k4k.inkquest.infra.enums;

/**
 * Используется для указания уровня выполнения этапов квеста:
 * FULL - выполнять все задачи;
 * REQUIRED - только обязательные
 */
public enum CompletionLevel {
    FULL, REQUIRED
}
